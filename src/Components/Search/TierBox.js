import React from 'react';

const TierBox = ({data}) => {
    const {
        type,
        rank,
        tier,
        win,
        lose
    } = data;
    return (
        <div></div>
    );
}

export default TierBox;